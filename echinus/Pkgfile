# Description: A lightweight and easily configurable tiling window manager. (git checkout)
# URL:         http://plhk.ru/
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git

name=echinus
version=git
release=3
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/onodera-punpun/echinus $name
		cd $name
	fi

	make
	make DESTDIR=$PKG PREFIX=/usr install
}
