# Description: Execute whatever you want when MPD (Music Player Daemon) changes its state. (git checkout)
# URL:         https://github.com/onodera-punpun/mpd_trigger
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  onodera, https://github.com/onodera-punpun/crux-ports/issues
# Depends on:  git libmpdclient

name=mpdtrigger
version=git
release=1
source=()

build () {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/onodera-punpun/mpd_trigger $name
		cd $name
	fi

	make
	install -Dm755 mpdtrigger $PKG/usr/bin/mpdtrigger
}
