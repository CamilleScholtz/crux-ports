# Description: A lightwieght floating wm with tiling capabilites. (git checkout)
# URL:         https://github.com/onodera-punpun/pegasus
# Packager:    onodera, onodera at openmailbox dot org
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git

name=pegasus
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/master
	else
		git clone git://github.com/onodera-punpun/pegasus $name
		cd $name
	fi

	make
	make DESTDIR=$PKG PREFIX=/usr install
}
