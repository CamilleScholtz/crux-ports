# Description: Lightweight event loop library.
# URL:         Lightweight event loop library
# Maintainer:  onodera, https://github.com/onodera-punpun/crux-ports/issues
# Depends on:  

name=libuev
version=1.4.2
release=1
source=(https://github.com/troglobit/$name/archive/v$version.tar.gz)

build () {
	cd $name-$version

	./autogen.sh
	./configure --prefix=/usr

	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share
}
