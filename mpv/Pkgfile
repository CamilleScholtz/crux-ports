# Description: General Purpose video player based on MPlayer and mplayer2
# URL:         https://mpv.io/
# Maintainer:  onodera, https://github.com/onodera-punpun/crux-ports/issues
# Depends on:  git libjpeg-turbo freetype fribidi
# Optional:    youtube-dl libplacebo

name=mpv
version=git
release=3
source=()

download_git() {
	if [[ -d $PKGMK_SOURCE_DIR/$name ]]; then
		git -C $PKGMK_SOURCE_DIR/$name fetch --depth=1
		git -C $PKGMK_SOURCE_DIR/$name clean -f
		git -C $PKGMK_SOURCE_DIR/$name reset --hard origin/$2
	else
		git -C $PKGMK_SOURCE_DIR clone --depth=1 $1 -b $2 $name
	fi

	cp -r $PKGMK_SOURCE_DIR/$name $PKGMK_WORK_DIR/src/$name
}

build() {
	download_git https://github.com/avih/mpv-build mpv-build-rewrite
	cd $name

	echo 'config_mpv_opts_config="--prefix=/usr"' >>user.conf
	./rebuild -j${JOBS-1}

	cd source/mpv
	./waf install --destdir=$PKG
}
